apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "koffan.fullname" . }}-test-connection"
  labels:
    {{- include "koffan.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "koffan.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
