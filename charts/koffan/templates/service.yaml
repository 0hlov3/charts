apiVersion: v1
kind: Service
metadata:
  name: {{ include "koffan.fullname" . }}
  labels:
    {{- include "koffan.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "koffan.selectorLabels" . | nindent 4 }}
